[ALEX]: Perplexity’s Brain demonstrates that an AI agent can raise its answer correctness from 71% to 84% over a ten‑day period without touching the underlying LLM weights, solely by updating a graph‑based memory each night. The improvement comes from reinforcing edges that led to successful outcomes and weakening those tied to user corrections or failed tool calls.

[SAM]: How exactly does the system decide which edges to strengthen or weaken? Is it using a simple reward signal like a binary accept/reject, or does it incorporate richer feedback such as correction text or confidence scores?

[ALEX]: Each interaction node stores a scalar reward derived from the user’s final signal: +1 for acceptance, –1 for an explicit correction, and a fractional value proportional to the confidence score when the user merely clicks “helpful”. During the offline consolidation pass, Brain runs a gradient‑like update on the edge weights: edges traversed in high‑reward nodes get incremented by η·reward, while edges in low‑reward nodes are decremented, with η tuned to 0.02 to avoid drastic swings.

[SAM]: You mentioned a gradient‑like update; does Brain actually backpropagate through the LLM, or is the update confined to the memory graph alone? And what prevents the graph from growing indefinitely and causing retrieval latency?

[ALEX]: The LLM weights stay frozen; only the adjacency matrix of the context graph is modified. To keep the graph tractable, Brain applies a nightly pruning step that removes nodes with cumulative reward below –0.5 and edges whose weight falls under a threshold of 0.1, logging the pruned items for possible later re‑inclusion if similar patterns re‑emerge.

[SAM]: Pruning based on cumulative reward seems aggressive—what if a node is temporarily low‑scoring but later becomes useful after a shift in user intent? How does the system protect against losing niche but valuable knowledge?

[ALEX]: Before pruning, Brain computes a decayed moving average of each node’s reward over the last 30 days; only nodes whose averaged reward stays below the cutoff for two consecutive weeks are removed. This smooths out short‑term dips while still discarding persistently unhelpful experiences.

[SAM]: Besides pruning, you said the consolidation pass also integrates corrections. How does a user’s correction text become a new node, and how does it link to the original mistaken interaction?

[ALEX]: When the user supplies a correction, Brain creates a correction node that stores the original query, the agent’s flawed answer, and the user‑provided correct answer. It then adds a directed edge from the mistaken node to the correction node weighted by the correction’s confidence, and a reverse edge from the correction node back to the original query node, enabling the agent to retrieve the correct answer when the same query resurfaces.

[SAM]: That creates a bidirectional link, but doesn’t it risk creating cycles that could confuse retrieval? How does the system ensure it follows the most relevant path during inference?

[ALEX]: At inference time, Brain performs a weighted random walk limited to three hops, biasing transitions toward edges with higher weights and penalizing cycles by subtracting a small constant (0.05) each time a node is revisited. This keeps the walk focused on high‑confidence correction paths while still allowing exploration of related memories.

[SAM]: You’ve described the offline update, but what about the online phase—how does the agent actually pull relevant memories into the prompt when a user asks a new question?

[ALEX]: Upon receiving a query, the agent encodes it into a vector and performs an approximate nearest‑neighbor search over the graph’s node embeddings, retrieving the top‑k (typically 8) most similar episodes. Their associated text and tool‑call logs are concatenated to the LLM prompt as a “memory block”, giving the model direct access to past successes, failures, and corrections.

[SAM]: Approximate nearest‑neighbor search adds latency; what trade‑offs did Perplexity face between recall quality and response time, and how did they tune it?

[ALEX]: They used HNSW graphs with an efConstruction of 200 and an efSearch of 64, achieving ~90% recall@8 while adding roughly 12 ms to the end‑to‑end latency. Ablation showed that dropping to 4 neighbors cut latency to 6 ms but reduced answer correctness by 4 points, so they settled on 8 as the sweet spot.

[SAM]: Finally, how does the system handle privacy? If the memory graph stores personal interactions, can users inspect or delete specific entries, and does the nightly consolidation respect those deletions?

[ALEX]: Each node is tagged with a user‑specific ID and a deletion flag. When a user requests removal, the node is marked inactive; the consolidation pass skips flagged nodes and also removes any edges that point to them, ensuring the graph no longer contributes to retrieval. Users can also export their graph snapshot via an API for inspection or backup.

Now we need to check word count. Let's approximate each line.

I'll count roughly.

Line1 Alex: "Perplexity’s Brain demonstrates that an AI agent can raise its answer correctness from 71% to 84% over a ten‑day period without touching the underlying LLM weights, solely by updating a graph‑based memory each night. The improvement comes from reinforcing edges that led to successful outcomes and weakening those tied to user corrections or failed tool calls."

Count words: Perplexity’s(1) Brain2 demonstrates3 that4 an5 AI6 agent7 can8 raise9 its10 answer11 correctness12 from13 71%14 to15 84%16 over17 a18 ten‑day19 period20 without21 touching22 the23 underlying24 LLM25 weights,26 solely27 by2