[ALEX]: When we attempted to answer a multi‑clause compliance query by stuffing the entire 120‑page regulation into a single prompt, the token count exceeded 115 k, driving the API cost to roughly $0.62 per call and adding 2.8 seconds of latency. The model’s attention was diluted across irrelevant sections, causing it to miss a critical exemption clause in 23 % of test runs. This brute‑force approach makes the system uneconomical for high‑volume use and reveals the core bottleneck: raw context window overload.

[SAM]: So the problem isn’t the model’s capability but the way we feed it information. If the regulation is that large, what determines which parts are actually needed for a given query, and how do we identify those parts without already knowing the answer?

[ALEX]: We start by extracting a lightweight summary or index of the document—think of it as a table of contents generated by a cheap embedding‑based retriever. This index lives outside the main prompt and is only a few hundred tokens. The main agent receives the user query, consults the index to locate the relevant sections (e.g., clauses 4.2‑4.5), and then pulls just those snippets into the context window. By limiting the input to the top‑ranked passages, we cut the token load from 115 k to under 4 k while preserving the information needed to answer correctly.

[SAM]: That sounds like a classic retrieval‑augmented generation pipeline, but you mentioned a “main agent” and a “subagent.” Where does the subagent fit in, and why not let the retriever itself be the subagent?

[ALEX]: The retriever is indeed a subagent: a lightweight, purpose‑built model whose sole job is to map a query to document IDs or passage spans. Because it operates on a much smaller corpus and uses a narrow prompt (just the query plus a few instruction tokens), it consumes maybe 200 tokens per call versus the tens of thousands the main agent would need. Delegating this lookup to a subagent frees the main agent to focus on reasoning over the retrieved snippets, keeping its own prompt tight and its token budget predictable.

[SAM]: If the subagent does the retrieval, what happens when the relevant information spans multiple, non‑contiguous sections or requires cross‑clause reasoning? Does the main agent still need to stitch those pieces together, and how does that affect token usage?

[ALEX]: The main agent receives a concatenated block of the top‑N retrieved passages—say, the three most relevant clauses—each preceded by a short heading that the subagent adds. Even if the reasoning requires comparing clauses 4.2 and 7.9, both are already in the context window because the subagent retrieved them based on semantic similarity. The main agent then performs the comparison within a single prompt, typically staying under 6 k tokens. If the answer needs more than the initial N passages, we iterate: the main agent asks the subagent for additional context, creating a tight feedback loop that adds only a few hundred tokens each round.

[SAM]: Iterating sounds like it could reintroduce latency if each round trips to the model. How do you bound the number of iterations, and what trade‑off do you accept between token savings and response time?

[ALEX]: We set a hard ceiling—usually two retrieval rounds—based on empirical data showing that >95 % of queries are resolved within that limit. Each extra round adds roughly 0.4 seconds of latency and ~250 tokens, which is still far cheaper than processing the full document. The trade‑off is a modest increase in latency for a massive reduction in cost; in our benchmarks, the two‑round system cut API spend by 78 % while keeping average latency under 1.2 seconds.

[SAM]: Beyond retrieval loop? Actually we need to talk about memory as well. We haven't covered memory yet. Let's incorporate memory in later exchanges.

We have done 6 exchanges (ALEX, SAM, ALEX, SAM, ALEX, SAM). Need more to reach 10-14. Let's continue with memory and self-managed memory, and the five-step methodology.

We have covered Step 1 (Get the context out of the window). Need to mention steps 2-5 briefly.

Let's continue.

Exchange 7:

[ALEX]: After retrieving the relevant snippets, Step 2 is to delegate the detailed execution to a specialized subagent. For instance, if the query asks for a risk score based on the retrieved clauses, we spawn a “risk‑evaluator” subagent that receives only those clauses plus a compact instruction set. This subagent can be a smaller, fine‑tuned model optimized for numerical reasoning, which further reduces token usage because it never sees the full regulation.

[SAM]: Why not let the main agent do the risk evaluation itself? Wouldn’t that avoid the overhead of spawning another call?

[ALEX]: The main agent’s prompt would then need to contain both the retrieval instructions and the risk‑evaluation logic, inflating its token count. By offloading the evaluation to a subagent tuned for that task, we keep the main agent’s prompt focused on orchestration—query analysis, subagent selection, and result synthesis—typically under 1 k tokens. The subagent, being smaller, can run faster and cheaper per token, and we can even cache its weights for repeated use.

[SAM]: That makes sense for compute, but how does the system avoid re‑doing the same risk