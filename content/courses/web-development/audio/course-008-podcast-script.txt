[ALEX]: You can ship a polished, responsive landing page that includes a 500vh hero video scrub, animated product grid, and expanding video reveals in under ten minutes without ever opening Figma, Framer, or Webflow—just by prompting Claude, feeding it a reference screenshot, and letting Fable 5 handle the animation primitives.  
[SAM]: How does the workflow start? Do you need any existing assets, or can you begin purely from a text description of the desired layout?  
[ALEX]: You begin with a screenshot of any Fable 5‑based site—even a rough wireframe—to give the AI a visual anchor. That image is fed to GPT Image 2 (or a comparable model) with a prompt like “make this scene 8k, replace the woman with a man, keep the lighting and composition.” The model returns a high‑resolution, royalty‑free illustration that matches the original structure but swaps the subject.  
[SAM]: Once you have that illustration, how do you strip away UI elements like text, icons, or buttons so the background is clean for video generation?  
[ALEX]: A second prompt tells the model to remove all overlays: “create an image like this but without any text, icons, buttons, or UI elements—just the pure background.” The output is a clean canvas that you then feed into an image‑to‑video tool such as Cdens2/Higgsville, prompting it to animate the scene (e.g., “slow dolly left, subtle particle drift”) to produce a short looping clip that will serve as the hero background.  
[SAM]: After you have the animated background, how does Claude actually generate the HTML, CSS, and JavaScript for the page, and where does Fable 5 come into play?  
[ALEX]: In a fresh Claude session you create a folder (e.g., “3d‑website”) and issue natural‑language commands like “set up an HTML5 boilerplate, link Fable 5 from the CDN, and create a full‑height hero section.” Claude writes the boilerplate, imports Fable 5’s CSS and JS, and scaffolds the markup. Because the session retains context, you can later ask Claude to tweak fonts, adjust section heights, or add a product grid without rewriting the whole file.  
[SAM]: What does the Fable 5 library provide out of the box for the kinds of animations we need—scroll‑driven video scrub, responsive grids, and expanding video reveals?  
[ALEX]: Fable 5 exports reusable components: a `<ScrollScrub>` syncs a video’s `currentTime` to the page’s scroll offset, a `<ResponsiveGrid>` uses CSS grid with auto‑fit columns and gap utilities, and an `<ExpandOnHover>` wrapper animates height/opacity when a user interacts with a video thumbnail. You import these components directly in the JSX‑like syntax Claude generates, then pass props such as video source, breakpoint values, or duration.  
[SAM]: Let’s dive into the hero section. How exactly do you achieve the 500vh scroll‑scrub effect, and what JavaScript is involved?  
[ALEX]: Claude sets the hero container to `height: 500vh; overflow: auto;` so the page scrolls five viewport heights. Inside, a `<video>` element is placed with `position: fixed; top: 0; left: 0; width: 100%; height: 100%; object-fit: cover;`. A scroll listener (or Fable 5’s built‑in hook) reads `window.scrollY`, maps it to a range of 0–video.duration, and updates `video.currentTime` on each animation frame via `requestAnimationFrame`. The video therefore plays forward when scrolling down and reverses when scrolling up, giving the scrub feel.  
[SAM]: Are there performance trade‑offs with a 500vh hero and a high‑resolution looping video, especially on mobile devices?  
[ALEX]: Yes—large video files can cause jank if not optimized. The workflow recommends exporting the Cdens2/Higgsville clip at 1080p, H.264, with a modest bitrate (~2.5 Mbps) and enabling `preload="metadata"` plus `playsinline`. You also lazy‑load the video until the hero enters the viewport using an IntersectionObserver, and you provide a low‑resolution poster image as a fallback. On very low‑end devices you can swap to a shorter, lower‑resolution clip or a static background via a media query.